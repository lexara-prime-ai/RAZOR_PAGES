@page
@model CollegeWebsite.Pages_Students.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>StudentModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="StudentModel.Id" />
            <div class="form-group">
                <label asp-for="StudentModel.Username" class="control-label"></label>
                <input asp-for="StudentModel.Username" class="form-control" />
                <span asp-validation-for="StudentModel.Username" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="StudentModel.Address" class="control-label"></label>
                <input asp-for="StudentModel.Address" class="form-control" />
                <span asp-validation-for="StudentModel.Address" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="StudentModel.Gender" class="control-label"></label>
                <input asp-for="StudentModel.Gender" class="form-control" />
                <span asp-validation-for="StudentModel.Gender" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
